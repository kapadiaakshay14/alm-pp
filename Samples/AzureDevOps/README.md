# Azure DevOps
This folder contains a set of sample Azure DevOps pipeline definitions that can be used to set up enterprise-ready ALM implementations for Power Platform. In addition it also contains associated helper PowerShell scripts that are used to enhance the ALM implementation for Power Platform if using Azure DevOps. They are triggered at various points within the pipeline definitions.
